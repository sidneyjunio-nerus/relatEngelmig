services:
  relatengelmig:
    image: ${GHCR_IMAGE}
    container_name: relatengelmig
    restart: unless-stopped
    env_file:
      - .env.client
    ports:
      - "${PORT:-3210}:3210"
    volumes:
      - "${CLIENT_PRINT_ROOT}:/data/print:ro"
